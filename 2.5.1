try-expert核心概念
try-except 是 Python 处理运行时异常的核心机制：
异常：程序运行时发生的错误（比如除以 0、访问不存在的列表索引、打开不存在的文件等），如果不处理，程序会立即终止并抛出错误信息。
try 块：包裹 “可能出现异常” 的代码，Python 会先执行这部分代码。
except 块：如果 try 块中的代码抛出异常，Python 会跳转到对应的 except 块执行 “补救逻辑”；如果没有异常，except 块会被跳过。
用法
1. 最简化写法（捕获所有异常）
2. 捕获指定类型的异常
3. 捕获异常并获取错误信息
4. 扩展：else 和 finally 子句
